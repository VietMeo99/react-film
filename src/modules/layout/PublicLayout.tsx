import React, { } from 'react';
// import { Loading } from '../components';

export const PublicLayout = (children: any, test: any) => {
  return (
    <div className="main">
      {/* <Suspense fallback={<Loading loading={true} className="u-text-center" />}> */}
      {/* <Suspense fallback={<div>Loading ... </div>}>
        {children}  
      </Suspense> */}
    </div>
  );
};

